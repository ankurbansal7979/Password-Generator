import React, { useState } from 'react'

function savePassword({password, isSaving}) {
    const [passwords, setPasswords] = useState([]);
  return (
    <div>
      
    </div>
  )
}

export default savePassword
